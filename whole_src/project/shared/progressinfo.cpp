#include "progressinfo.h"

using namespace nq;

//ProgressInfo::ProgressInfo(qint64 id)
//    : id(id), status(TPS_Success), min(0), max(100), pos(100), message("")
//{

//}

//ProgressInfo::ProgressInfo(qint64 id, Status_e status, qint32 min, qint32 max, qint32 pos, const QString &message)
//    : id(id), status(status), min(min), max(max), pos(pos), message(message) {
//}

//ProgressInfo::ProgressInfo(ProgressInfo &pi)
//{
//    this->id = pi.id;
//    this->status = pi.status;
//    this->min = pi.min;
//    this->max = pi.max;
//    this->pos = pi.pos;
//    this->message = pi.message;
//}

//ProgressInfo &ProgressInfo::operator=(const ProgressInfo &pi)
//{
//    this->id = pi.id;
//    this->status = pi.status;
//    this->min = pi.min;
//    this->max = pi.max;
//    this->pos = pi.pos;
//    this->message = pi.message;
//    return *this;
//}

